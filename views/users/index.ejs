<div class="container">
    <div class="users">

<h2>Users - Overview</h2>

<table class="user-table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Address</th>
            <th>Email</th>
            <th>Role</th>
        </tr>
    </thead>
    <tbody>

        <% users.forEach(user => { %>
        <tr>
            <td>
                <a href="<%= `/users/${user._id}` %>">
                    <%= user.fullName %>
                </a>
            </td>
            <td><%= user.fullAddress %></td>
            <td><%= user.email %></td>
            <td><%= user.role %></td>
            <td>
               <button type="submit" class="btn first"><a href="<%= `/users/${user._id}/edit` %>">Edit</a></button>
            </td>
            <td>
                <button type="submit" class="btn first"> <a href="<%= `/users/${user._id}/delete?_method=DELETE` %>"
                    onclick="return confirm('Are you sure you want to delete this record?') " >Delete</a></button>
            </td>
        </tr> 
        <% }); %>
    </tbody>
</table>
    <a class="user-btn" href="/users/new">Add User</a>
</div>